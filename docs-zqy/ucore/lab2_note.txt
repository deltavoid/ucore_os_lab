

problem

1. 虚拟地址空间KERNBASE ~ KERNBASE + MEMSIZE 线性的映射到物理地址空间kern_entry ~ kern_entry + MEMSIZE

2. 虚拟地址空间到物理地址空间不是直接映射，使得处理页表变得非常麻烦。
    同一个东西（页表）维护了两套指针，物理指针（处理器使用）和虚拟地址指针（内核使用）

3. 已经存在页表，为什么还要物理内存分配？默认可以通过映射将不连续的页变为连续的页。那么物理内存连续分配的意义是什么？优化缓存吗？



